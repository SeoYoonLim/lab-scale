[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: dev DB(localhost:15432)에 붙는 테스트. DB가 꺼져 있으면 skip된다.
    slow: Ollama LLM을 실제로 호출하는 비결정적 테스트. 기본 실행에서 제외, -m slow로만 실행한다.
addopts = -m "not slow"
